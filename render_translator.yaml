services:
  - type: web
    name: ai-translator
    env: python
    buildCommand: pip install -r translator_requirements.txt
    startCommand: gunicorn translator_app:app
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: PYTHON_VERSION
        value: 3.11.0
    plan: free

